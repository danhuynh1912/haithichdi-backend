DJANGO_DEBUG=0

# Production mode A (lowest runtime network cost):
# inject secrets directly into Lambda environment variables.
# DJANGO_SECRET_KEY=replace-me

POSTGRES_DB=tours_db
POSTGRES_USER=tours_user
POSTGRES_HOST=replace-me
POSTGRES_PORT=5432
# POSTGRES_PASSWORD=replace-me

# Production mode B (runtime reads from SSM Parameter Store SecureString):
# keep SSM interface endpoint enabled in VPC to avoid cold-start failures.
# DJANGO_SECRET_KEY_SSM_PARAMETER=/haithichdi/prod/backend/django_secret_key
# POSTGRES_PASSWORD_SSM_PARAMETER=/haithichdi/prod/backend/postgres_password

USE_S3=1
AWS_STORAGE_BUCKET_NAME=haithichdi-prod-media
AWS_S3_REGION_NAME=ap-southeast-1
AWS_S3_CUSTOM_DOMAIN=replace-me.cloudfront.net

# Public frontend origins allowed to call the backend API.
CORS_ALLOWED_ORIGINS=https://main.example.amplifyapp.com
CSRF_TRUSTED_ORIGINS=https://main.example.amplifyapp.com

# Leave these unset in AWS when Lambda or ECS already uses IAM roles.
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=

# MinIO / local-only settings. Do not set these in AWS production.
# AWS_S3_ENDPOINT_URL=
# AWS_S3_PUBLIC_ENDPOINT_URL=
# AWS_S3_PUBLIC_BASE_URL=
# AWS_S3_ADDRESSING_STYLE=path
